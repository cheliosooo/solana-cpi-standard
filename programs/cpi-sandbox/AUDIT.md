# Sandbox instruction audit

Scope: `execute_cpis`, its Anchor account constraints, the extracted core dispatcher/registry, and the test runner. Applied the `anchor-instruction-audit` checklist from the reference repository. No unresolved Critical or Minor findings were identified within this sandbox's test-only authority model.

| Area                       | Result                                                                                                                                                                                                                                               |
| -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Signer and authority       | `payer` is an Anchor `Signer`. There is no shared administrator or vault authority. The caller can authorize only its own payer-scoped PDA. The integration suite checks missing signer and wrong-PDA rejection.                                     |
| PDA derivation             | Anchor checks `[b"cpi-sandbox", payer.key()]` with a canonical bump; the handler uses `ctx.bumps.pda`. No caller-supplied bump, unchecked authority, or initialization state is trusted.                                                             |
| Account constraints / ATAs | The sandbox declares no token account fields. Each downstream protocol validates its own token ownership, mint, ATA, and sysvar constraints. The sandbox supplies no asset-accounting guarantees.                                                    |
| CPI targets                | Every CPI resolves through the Rust registry. Unknown/retired IDs and wrong target programs are rejected. Raw Jupiter/Metaplex bytes retain the standard's existing permissive behavior, authorized only with the caller's own signer/PDA.           |
| Arithmetic and decoding    | Borsh rejects malformed/trailing outer bytes. The core validates lengths, argument prefixes and u8 indices. Tracked-account bounds are checked before invocation. No financial arithmetic is performed by the sandbox.                               |
| Skip slots                 | Client-only execution rejects any skip before a CPI runs. The amount dispatcher requires exactly one skip and supplies one serialized u64.                                                                                                           |
| Cross-instruction state    | There is one instruction and no persisted state, price cache, or cached token accounts. No account reload, freshness, insolvency, delegate, or circuit-breaker checks are applicable to this generic test authority.                                 |
| Duplicate accounts         | Deduplicated indices and duplicate account positions are intentional in the wire format. Downstream instructions enforce any required source/destination distinction.                                                                                |
| Deployment / upgrades      | The runner installs only this sandbox binary into an HTTP loopback Surfpool. It does not deploy to a cluster, change user wallet configuration, or load a user's wallet. The checked-in keypair is the public local test fixture from the reference. |
| External mint authorities  | Real mainnet program/mint definitions are forked locally. Local token balances and farm accrued rewards are explicitly seeded test state. No claim is made about production mint freeze or upgrade authority policy.                                 |

Validated with the SBF sandbox running on Surfpool, including real Jupiter, Perena, Kamino lending/farms, and Metaplex program invocations. Native unit tests alone do not establish runtime behavior; the integration tests exercise the compiled sandbox.
